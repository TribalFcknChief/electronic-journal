import { Grid2} from "@mui/material";
import styled from "styled-components";

export const ProgressPageWrapper = styled(Grid2)(() => ({
  width: "100vw",
  height: "100vh",
  overflowY: "hidden",
}));
